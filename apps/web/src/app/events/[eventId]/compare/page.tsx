import { AppShell, PageHeader } from "@/components/app-shell";
import { createClient } from "@/lib/supabase/server";
export default async function ComparePage({params,searchParams}:{params:Promise<{eventId:string}>;searchParams:Promise<{a?:string;b?:string}>}){const{eventId:eventKey}=await params;const{a,b}=await searchParams;const supabase=await createClient();const{data:event}=await supabase.from('events').select('id,name').eq('event_key',eventKey).maybeSingle();const{data:rows}=event?await supabase.from('event_teams').select('team_id,teams(team_number,name)').eq('event_id',event.id):{data:[]};const pick=(id?:string)=>(rows??[]).find((row:any)=>String(row.teams?.team_number)===id) as any;return <AppShell active="Teams"><PageHeader eyebrow={event?.name??'Comparison'} title="Compare teams."/><form className="card form-grid" action={`/events/${eventKey}/compare`}><div className="field"><label>Team A</label><select name="a" defaultValue={a}>{(rows??[]).map((row:any)=><option key={row.team_id} value={row.teams?.team_number}>{row.teams?.team_number} · {row.teams?.name}</option>)}</select></div><div className="field"><label>Team B</label><select name="b" defaultValue={b}>{(rows??[]).map((row:any)=><option key={row.team_id} value={row.teams?.team_number}>{row.teams?.team_number} · {row.teams?.name}</option>)}</select></div><button className="button">Compare</button></form>{a&&b&&<section className="card section"><h2>{pick(a)?.teams?.name??a} vs {pick(b)?.teams?.name??b}</h2><p className="muted">Team-scoped data, pit scouting, match-history totals, and live scoring signal are available from each team page. Visual overlays are the next analytics layer.</p></section>}</AppShell>}
