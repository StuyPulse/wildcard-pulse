import Link from "next/link";
import { AppShell, PageHeader } from "@/components/app-shell";
import { createClient } from "@/lib/supabase/server";
import { LiveRefresh } from "@/components/live-refresh";
import { calculateScoutStats, formatStat } from "@/lib/scouting-stats";

type PageProps={params:Promise<{eventId:string}>;searchParams:Promise<{sort?:string;dir?:string}>};
const asNumber=(value:unknown)=>Number.isFinite(Number(value))?Number(value):0;
function tbaMetric(ranking:any, info:any[], pattern:RegExp) { const index=info.findIndex((metric)=>pattern.test(metric.name)); return index>=0?asNumber(ranking?.sort_orders?.[index]):0; }
export default async function SummaryPage({params,searchParams}:PageProps){const{eventId:eventKey}=await params;const query=await searchParams;const supabase=await createClient();const{data:event}=await supabase.from("events").select("id,name,event_key").eq("event_key",eventKey).maybeSingle();if(!event)return <AppShell active="Summary"><PageHeader eyebrow="Event summary" title="Event unavailable."/><section className="card"><p className="muted">This event could not be found.</p></section></AppShell>;const[{data:eventTeams},{data:entries}]=await Promise.all([supabase.from("event_teams").select("team_id,teams(team_number,name)").eq("event_id",event.id),(supabase as any).from("scouting_entries").select("team_id,payload").eq("event_id",event.id).eq("entry_type","match").eq("status","submitted")]);let rankings:any[]=[];let sortInfo:any[]=[];let oprs:Record<string,number>={};let apiError="";if(!process.env.TBA_AUTH_KEY)apiError="TBA_AUTH_KEY is unavailable to this deployment.";else try{const headers={"X-TBA-Auth-Key":process.env.TBA_AUTH_KEY};const [rankingsResponse,oprsResponse]=await Promise.all([fetch(`https://www.thebluealliance.com/api/v3/event/${event.event_key}/rankings`,{headers,cache:"no-store"}),fetch(`https://www.thebluealliance.com/api/v3/event/${event.event_key}/oprs`,{headers,cache:"no-store"})]);const rankingPayload=await rankingsResponse.json();const oprPayload=await oprsResponse.json();if(rankingsResponse.ok&&Array.isArray(rankingPayload?.rankings)){rankings=rankingPayload.rankings;sortInfo=rankingPayload.sort_order_info??[];oprs=oprPayload?.oprs??{};}else apiError=`TBA returned HTTP ${rankingsResponse.status}.`;}catch{apiError="Could not reach TBA right now."}const tbaByTeam=new Map(rankings.map((ranking)=>[Number(String(ranking.team_key??"").replace("frc","")),ranking]));const rows=(eventTeams??[]).map((link:any)=>{const reports=(entries??[]).filter((entry:any)=>entry.team_id===link.team_id);const tba=tbaByTeam.get(link.teams?.team_number);const stats=calculateScoutStats(reports);return{team:link.teams,teamId:link.team_id,tba,oprs,stats,tbaTotalFuel:tbaMetric(tba,sortInfo,/total.*fuel|avg.*match/i),tbaAutoFuel:tbaMetric(tba,sortInfo,/auto.*fuel/i),tbaTeleopFuel:tbaMetric(tba,sortInfo,/teleop.*fuel/i)};});const metric=(row:any,key:string)=>key==="team"?row.team?.team_number:key==="rank"?asNumber(row.tba?.rank||99999):key==="opr"?asNumber(row.oprs[`frc${row.team?.team_number}`]):key in row.stats?asNumber(row.stats[key]):asNumber(row[key]);const sort=query.sort??"rank";const dir=query.dir==="asc"?1:-1;rows.sort((a:any,b:any)=>{const diff=metric(a,sort)-metric(b,sort);return sort==="team"||sort==="rank"?diff:diff*dir;});const header=(key:string,label:string)=><Link href={`/events/${eventKey}/summary?sort=${key}&dir=${sort===key&&query.dir!=="asc"?"asc":"desc"}`}>{label}</Link>;const column=(key:string,label:string)=><th key={key}>{header(key,label)}</th>;const columns=[['autoMaxScored','Auto max scored'],['autoMaxFerried','Auto max ferried'],['autoAvgScored','Auto avg scored'],['autoAvgFerried','Auto avg ferried'],['teleopMaxScored','Teleop max scored'],['teleopMaxFerried','Teleop max ferried'],['teleopAvgScored','Teleop avg scored'],['teleopAvgFerried','Teleop avg ferried'],['avgFouls','Avg fouls'],['defense','Defense'],['brokenPercent','% broken']] as const;return <AppShell active="Summary"><LiveRefresh tables={["scouting_entries"]}/><PageHeader eyebrow={event.name} title="Scouting summary."/><section className="card"><div className="card-head"><div><h2>Live team data</h2><p className="muted">TBA and internal scouting data. All displayed values round to two decimals; headings sort the table.</p></div><div className="row-actions"><Link className="button secondary" href={`/events/${eventKey}/compare`}>Compare</Link><Link className="button secondary" href={`/events/${eventKey}/teams`}>Teams</Link></div></div>{apiError&&<p className="error">{apiError}</p>}<div className="table-scroll"><table><thead><tr>{column("rank","Rank")}{column("team","Team")}<th>W-L-T</th>{column("opr","OPR")}{column("tbaTotalFuel","TBA total fuel")}{column("tbaAutoFuel","TBA auto fuel")}{column("tbaTeleopFuel","TBA teleop fuel")}{column("entries","Reports")}{columns.map(([key,label])=>column(key,label))}<th/></tr></thead><tbody>{rows.map((row:any)=><tr key={row.teamId}><td>{row.tba?.rank??"—"}</td><td><strong>{row.team?.team_number} · {row.team?.name}</strong></td><td>{row.tba?.record?`${row.tba.record.wins}-${row.tba.record.losses}-${row.tba.record.ties}`:"—"}</td><td>{formatStat(asNumber(row.oprs[`frc${row.team?.team_number}`]))}</td><td>{formatStat(row.tbaTotalFuel)}</td><td>{formatStat(row.tbaAutoFuel)}</td><td>{formatStat(row.tbaTeleopFuel)}</td><td>{row.stats.entries}</td>{columns.map(([key])=><td key={key}>{formatStat(row.stats[key as keyof typeof row.stats])}</td>)}<td><Link className="link" href={`/events/${eventKey}/teams/${row.team?.team_number}`}>Open →</Link></td></tr>)}</tbody></table></div></section></AppShell>}
