import { notFound, redirect } from "next/navigation";
import { AppShell, PageHeader } from "@/components/app-shell";
import { LiveRefresh } from "@/components/live-refresh";
import { createClient } from "@/lib/supabase/server";
import { getViewerContext, viewerCanManage } from "@/lib/viewer-context";
import { PicklistBoard } from "./picklist-board";

export default async function PicklistPage({ params }: { params: Promise<{ eventId: string }> }) { const { eventId: eventKey } = await params; const [viewer, supabase] = await Promise.all([getViewerContext(), createClient()]); const canPick = viewer?.role === "strategist" || viewer?.role === "master" || viewerCanManage(viewer); if (!viewer?.organizationId || !canPick) redirect("/dashboard"); const { data: event } = await supabase.from("events").select("id,name").eq("event_key", eventKey).eq("organization_id", viewer.organizationId).maybeSingle(); if (!event) notFound(); const [{ data: categoryRows }, { data: eventTeamRows }, { data: rankingRows }] = await Promise.all([(supabase as any).from("picklist_categories").select("id,name,color,sort_order").eq("organization_id", viewer.organizationId).order("sort_order").order("name"), supabase.from("event_teams").select("team_id,teams(id,team_number,name)").eq("event_id", event.id), (supabase as any).from("picklist_rankings").select("id,team_id,category_id,rank,note,user_id").eq("event_id", event.id)]); const teams=(eventTeamRows??[]).map((row:any)=>row.teams).filter(Boolean).sort((a:any,b:any)=>a.team_number-b.team_number); return <AppShell active="Picklist"><LiveRefresh tables={["picklist_categories","picklist_rankings"]}/><PageHeader eyebrow={event.name} title="Picklist."/><PicklistBoard organizationId={viewer.organizationId} eventId={event.id} userId={viewer.userId} categories={categoryRows??[]} teams={teams} rankings={rankingRows??[]}/></AppShell>; }
