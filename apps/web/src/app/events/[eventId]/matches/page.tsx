import Link from "next/link";
import { AppShell, PageHeader } from "@/components/app-shell";
import { createClient } from "@/lib/supabase/server";
export default async function MatchesPage({params}:{params:Promise<{eventId:string}>}) { const {eventId}=await params; const supabase=await createClient(); const [{data:matches},{data:eventTeams}]=await Promise.all([supabase.from("matches").select("id,tba_match_key,match_number,match_type,red_teams,blue_teams,scheduled_at,status").eq("event_id",eventId).order("scheduled_at"),supabase.from("event_teams").select("team_id,teams(team_number)").eq("event_id",eventId)]); const teamNumbers=new Map((eventTeams??[]).map((row:any)=>[row.team_id,row.teams?.team_number])); const displayAlliance=(teamIds:string[])=>teamIds.map(id=>teamNumbers.get(id)??"—").join(" · "); const displayMatch=(key:string,number:number,type:string)=>key.startsWith("legacy:")?(type==="qualification"?"Q":"P")+number:key.slice(key.lastIndexOf("_")+1).toUpperCase().replace(/^QM/,"Q").replace(/M/g," M"); return <AppShell active="Matches"><PageHeader eyebrow="Event schedule" title="Matches."/><section className="card"><div className="card-head"><h2>Match schedule</h2><Link className="button secondary" href={`/events/${eventId}/teams`}>Team directory</Link></div>{matches?.length ? matches.map(m=><div className="match-row" key={m.id}><span className="match-time">{m.scheduled_at ? new Date(m.scheduled_at).toLocaleTimeString([], {hour:"numeric",minute:"2-digit"}) : "—"}</span><span className="match-num">{displayMatch(m.tba_match_key,m.match_number,m.match_type)}</span><div className="alliances"><div className="red">{displayAlliance(m.red_teams)}</div><div className="blue">{displayAlliance(m.blue_teams)}</div></div><span className="tag pending">{m.status}</span></div>) : <p className="muted">The schedule will appear after the event is imported.</p>}</section></AppShell>; }
