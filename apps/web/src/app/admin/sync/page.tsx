import { AppShell, PageHeader } from "@/components/app-shell";
import { TbaImportForm } from "@/components/admin-forms";
export default function SyncPage(){return <AppShell active="Events"><PageHeader eyebrow="Administration" title="Import an event from TBA."/><div className="page-grid"><section className="card"><h2>Import in three steps</h2><ol className="muted steps"><li>Find the event on The Blue Alliance.</li><li>Copy its key from the URL — everything after <code>/event/</code>.</li><li>Paste it below and select <strong>Import from TBA</strong>.</li></ol><p className="muted" style={{lineHeight:1.7}}>Each import adds or updates one event, its dates, teams, and schedule. Importing the same key again is safe; unchanged TBA data is skipped using its ETag.</p><div className="section" style={{maxWidth:500}}><TbaImportForm/></div></section><aside className="card"><h2>After importing</h2><p className="muted" style={{lineHeight:1.7}}>Open the event from the Events page to review its schedule and teams. If you imported the wrong event, use the red Delete button there. Events with scouting submissions cannot be deleted, protecting collected data.</p></aside></div></AppShell>}
