import { AppShell, PageHeader } from "@/components/app-shell";
import { TbaImportForm } from "@/components/admin-forms";
export default function SyncPage(){return <AppShell active="Events"><PageHeader eyebrow="Administration" title="Event sync."/><div className="page-grid"><section className="card"><h2>The Blue Alliance import</h2><p className="muted" style={{lineHeight:1.7}}>Imports event dates, teams, event-team links, and schedule through a server action. The TBA key stays on the server; repeated event responses use the stored ETag to avoid needless work.</p><div className="section" style={{maxWidth:430}}><TbaImportForm/></div></section><aside className="card"><h2>Phase 1 scope</h2><p className="muted" style={{lineHeight:1.7}}>Online-only, direct submissions, and Realtime-ready tables. No PWA cache, QR relay, or native sync queue is included yet.</p></aside></div></AppShell>}
