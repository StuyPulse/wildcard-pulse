import { AppShell, PageHeader } from "@/components/app-shell";
import { matches, teams } from "@/lib/demo-data";
export default function DemoPage() { return <AppShell><div className="demo-banner">Preview mode — this representative data is local only. Sign in to work with secured event data.</div><PageHeader eyebrow="Friday · Qualification 18" title="Good morning, PulseCrew."/><div className="grid stats">{[["27", "Submissions received", "+ 6 since last match"],["94%", "Scout coverage", "3 assignments remaining"],["42", "Teams at this event", "All teams imported"],["96%", "Data reliability", "Across 168 entries"]].map(([value,label,trend]) => <div className="card" key={label}><div className="stat-label">{label}</div><div className="stat-value">{value}</div><div className="trend">{trend}</div></div>)}</div><div className="grid dashboard-grid"><section className="card"><div className="card-head"><h2>Picklist signal</h2><a className="link" href="/events/demo/teams">Open comparison →</a></div><table><thead><tr><th>RANK</th><th>TEAM</th><th>OPR</th><th>RELIABILITY</th><th>TIER</th></tr></thead><tbody>{teams.map(t=><tr key={t.number}><td className="rank">{t.rank}</td><td><span className="team">{t.number}</span><span className="muted"> · {t.name}</span></td><td className="score">{t.opr}</td><td>{t.reliability}%</td><td><span className="tag complete">{t.tier}</span></td></tr>)}</tbody></table></section><section className="card"><div className="card-head"><h2>Match flow</h2><a className="link" href="/events/demo/matches">Schedule →</a></div>{matches.map(m=><div className="match-row" key={m.number}><span className="match-time">{m.time}</span><span className="match-num">Q{m.number}</span><div className="alliances"><div className="red">{m.red}</div><div className="blue">{m.blue}</div></div><span className={`tag ${m.status === "Live" ? "live" : "pending"}`}>{m.status}</span></div>)}</section></div></AppShell>; }
